To run the application, you will need a Django settings file called `settings.ini`. Copy the template, `public_settings.ini` and rename if `settings.ini` (leave the template as is).

Next, fill out the empty property values as needed to run the application.